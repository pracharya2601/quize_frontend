import { PLAY_QUIZE, GET_SINGLE_QUIZE, SUBMIT_ANSWER } from "./types";

export const dataReducer = (state, action) => {
    switch(action.type) {
        case PLAY_QUIZE: 
        return state;
        default: 
            return state;
    }
}